#include<stdio.h>
int addition(int,int);
main()
{
	int n,i,sum=0;
	printf("enter n value");
	scanf("%d",&n);
	c=addition(n,sum,i);
	printf("the result is %d",n,sum);
}
int addition(int n)
{
	return sum=0;
}
